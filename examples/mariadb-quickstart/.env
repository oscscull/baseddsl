# The MariaDB the engine (and `based migrate apply`) runs against. This default matches the
# throwaway Docker server in the README; edit it to point at your own server.
DATABASE_URL=mysql://root:based_test_pw@127.0.0.1:3307/based_test
