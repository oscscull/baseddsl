# The Postgres the engine (and `based migrate apply`) runs against. This default matches the
# throwaway Docker server in the README; edit it to point at your own server.
DATABASE_URL=postgres://postgres:based_test_pw@127.0.0.1:5433/based_test
